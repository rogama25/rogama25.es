---
import {Icon} from "astro-icon/components";

interface Props {
  name: string;
}
const { name } = Astro.props;
---
<Icon name={name} class="w-24 h-24 hover:scale-125 transition-transform duration-200 max-md:w-4/5 max-md:h-4/5"/>
